# Local venv (.venv_local): SQLite dev, no PostgreSQL driver.
# Full stack: pip install -r requirements.txt

# ===== Web =====
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ===== DB / ORM (SQLite only) =====
SQLAlchemy==2.0.35
alembic==1.13.3

# ===== Config / auth =====
pydantic==2.9.2
pydantic-settings==2.5.2
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

# ===== CV / ML =====
numpy==1.26.4; python_version < "3.13"
numpy>=2.1.0; python_version >= "3.13"
opencv-python-headless>=4.10.0.84
Pillow>=10.4.0
PyYAML>=6.0
# ultralytics 8.3.x needs numpy<2 — only on Python 3.10-3.12; on 3.13 use fallback detector or install torch+ultralytics manually
ultralytics==8.3.2 ; python_version < "3.13" and (platform_system != "Darwin" or platform_machine != "arm64")

# ===== Reports =====
reportlab==4.2.2
openpyxl==3.1.5

# ===== Logging =====
python-json-logger==2.0.7

# ===== WLED mDNS =====
zeroconf==0.146.2

# ===== Tests =====
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
